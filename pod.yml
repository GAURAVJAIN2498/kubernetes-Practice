apiVersion: v1
kind: Pod  
metadata:                           
  name: pod1                  
spec:                                    
  containers:                      
  - name: c1                     
    image: ubuntu              
    command: ["/bin/bash", "-c", "while true; do echo Hello!!; sleep 5 ; done"]
  restartPolicy: Never
